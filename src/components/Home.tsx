import Heroimg from './Heroimg';
import Menu from './Menu/Menu';

type Props = {};

const Home = (props: Props) => {
  return (
    <div>
      <Heroimg />
      <Menu />
    </div>
  );
};

export default Home;
